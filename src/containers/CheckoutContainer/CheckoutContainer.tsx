import { MiddlePositionContaner } from "@/components";

export const CheckoutContainer = () => {
    return (
        //TODO check others checkout pages
        <MiddlePositionContaner>a</MiddlePositionContaner>
    );
};
