export const GET = async () => {
    return Response.json({ a: 1 });
};

export const UPDATE = async () => {
    return Response.json({ a: 1 });
};

export const DELETE = async () => {
    return Response.json({ a: 1 });
};
